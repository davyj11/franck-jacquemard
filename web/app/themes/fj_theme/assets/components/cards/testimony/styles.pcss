.card-review {
    @apply border border-primary bg-white h-full p-6 lg:p-10 flex flex-col;

    .rating {
        &__icon {
            & + i {
                @apply ml-2;
            }
        }
    }
}
