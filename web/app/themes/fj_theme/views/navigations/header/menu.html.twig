{% if menu %}
    <ul class="menu" js-menu>
        {% for item in menu %}
            <li class="{{ isChildren ? item.classes | join(' ') : ' ' ~ item.classes | join(' ') }}">
                {% if 'menu-item-has-children' in item.classes %}
                    {% set icon = '🡒' %}
                {% else %}
                    {% set icon = '' %}
                {% endif %}
                <a target="{{ item.target }}" href="{{ item.link }}"><span class="item-index">0{{ loop.index }} -</span>{{ item.title }}</a>
                {% include "navigations/header/menu.html.twig" with {'menu': item.get_children, 'isChildren': true} %}
            </li>
        {% endfor %}
    </ul>
{% endif %}
